// 7. array index error

let foo = ['a', 'b', 'c'];
console.log(foo.length);
// = 3
console.log(foo[3]);
/**
 * = undefined
 * this will not result in an error,
 * log will output undefined since given
 * index value has no corresponding element
 */

// 8. create array
let names = ['buster', 'bleu', 'puff', 'tinkerbell', 'crush'];
