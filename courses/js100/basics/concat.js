// 1. concat first and last name together

const firstName = 'Josh';
const lastName = 'Rodriguez';
const fullName = firstName.concat(' ', lastName);
const alternative = `${firstName} ${lastName}`;
const additional = firstName + ' ' + lastName;
// = Josh Rodriguez
