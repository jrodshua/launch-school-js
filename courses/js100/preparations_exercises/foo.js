// 3. Create foo.js

let foo = 'bar';
console.log(foo);
foo;

// 4. Outputs
/**
 * node foo.js: bar
 * node REPL: undefined, bar, undefined, 'bar'
 * Chrome console: undefined, bar, undefined, 'bar'
 */
