// 4. given list of nums, display largest numeric value in list

/**
 * list of nums
 * 1, 6, 3, 2 (max = 6)
 * -1, -6, -3, -2 (max = -1)
 * 2, 2 (max = 2)
 */

let first = Math.max(1, 6, 3, 2);
let second = Math.max(-1, -6, -3, -2);
let third = Math.max(2, 2);

console.log(first, second, third);
